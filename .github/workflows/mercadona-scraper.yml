name: Diagnóstico estructura del repo

on:
  workflow_dispatch:

jobs:
  list-files:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Mostrar estructura real
        run: |
          echo "Estoy en:"
          pwd
          echo "Contenido del repo:"
          ls -la
          echo "Búsqueda completa:"
          find .
